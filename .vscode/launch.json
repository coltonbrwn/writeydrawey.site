{
    "configurations": [
        {
            "type": "node-terminal",
            "env": {
                "node_env": "dev"
            },
            "name": "webserver",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}